
/**
 * The running mode of the simulation (daily or until the end).
 */
public enum Mode {
    /** Run the simulation for a single day. */
    SINGLE_DAY,
    /** Run the simulation to the end. */
    FAST_FORWARD
}
